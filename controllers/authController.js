module.exports.signup_get = (res, req) => {
    res.render('signup');
};

module.exports.login_get = (res, req) => {
    res.render('signup');
};

module.exports.signup_post = (res, req) => {
    res.render('signup');
};

module.exports.login_post = (res, req) => {
    res.render('signup');
};